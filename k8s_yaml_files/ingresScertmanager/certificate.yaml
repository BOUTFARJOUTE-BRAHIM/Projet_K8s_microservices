apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate-tls
spec:
  secretName: certificate-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    -  ittech.work.gd
